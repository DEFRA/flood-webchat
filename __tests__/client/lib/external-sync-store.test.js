describe('ExternalSyncStore', () => {
  // A subscribed listener is notified of changes when the value is updated
  // A listener is unsubscribed if the function returned by the subscribe call that added it is itself called
  // The create function returns the result of a call to use external store and bound updater function
})
